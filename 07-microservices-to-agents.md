# 07 ｜ 从微服务到智能体——AI 如何改变技术架构

2016 年我在一家创业公司做后端。当时我们经历了从单体到微服务的迁移，整个团队花了将近八个月。那时候的共识是：微服务是「现代架构」的同义词。如果你的系统不是微服务，你的架构就是过时的。

九年后的现在，一个新的范型正在浮现。它的名字还很模糊——有人叫它「Agent 架构」，有人叫它「AI-native 架构」——但它的影响力很可能不亚于微服务当年对 SOA 的冲击。因为这一次，变化的不只是「怎么组织代码」，而是「代码运行的本质」变了。

## 微服务的隐含前提正在被打破

微服务架构的核心思想是：把系统拆成一系列独立部署、独立扩展、通过定义良好的 API 通信的小服务。这个思想的两个核心前提是：第一，人类需要理解和维护这些服务，所以每个服务不能太复杂。第二，服务之间的交互是确定的——你调一个 API，你知道会得到什么返回。

这两个前提在 Agent 架构面前都动摇了。

先说第一个。Agent 架构里的一个「智能体」不是一个你手写好行为规则的组件。它是一个拥有自主推理能力的实体——你给它一个目标，它自己决定采取什么行动。这意味着它的行为不是预定义的，而是动态生成的。一个传统的微服务，你知道它有十个 API endpoint，每个 endpoint 的行为是确定的。一个 Agent，你只知道它有一个目标，但它可能调用数百个不同的工具、做出你从未预期的决策路径。

这对架构设计意味着什么？意味着你对系统行为的可控性大幅下降了。在微服务架构里，你通过 API contract 和 service mesh 来管理行为。在 Agent 架构里，这些手段还在，但它们只能管到 Agent 的「工具接口」层面，管不到 Agent 的「决策逻辑」。你没办法用一个 OpenAPI spec 来约束一个 LLM 在拿到用户输入后「不应该调用那个工具」。

再看第二个前提：确定性交互。微服务之间的通信是确定性的——你发一个 HTTP request 给订单服务，它要么返回成功、要么返回失败、要么超时。这三种结果是你可以处理的。

但 Agent 之间的交互不是这样的。两个 Agent 之间的对话可能产生完全不可预测的结果。一个 Agent 说「我需要确认这个订单的库存状态」，另一个 Agent 回「库存确认需要先经过风控检查，你风控过了吗？」第一个 Agent 可能接着说「我没考虑过风控，让我查一下风控规则。」这个对话可以不断地分叉下去，每一轮都产生新的不确定性。

这不是 bug，这是 feature。Agent 架构的价值恰好在于它能处理微服务架构处理不了的不确定性任务。但这些不确定性对可观测性、调试、故障定位带来的挑战是前所未有的。

## 这不是又一个「新框架」

很多人听到「Agent 架构」，第一反应是「又一个要学的框架」。LangChain、AutoGPT、CrewAI——确实有一批框架在试图标准化 Agent 的开发模式。但我认为更值得关注的变化不在框架层面，而在架构原则层面。

回顾历史，每一次架构范型的切换都伴随着一套新的「设计原则」替代旧的一套。从单体到 SOA：耦合和内聚的原则变了。从 SOA 到微服务：服务边界划分的原则变了。从微服务到 Agent 架构：什么原则在失效，什么原则在增强？

**在失效的原则：**

「单一职责原则」——一个服务只做一件事。在 Agent 架构里，一个 Agent 的职责是「达成某个目标」，而这个目标可能天然跨域。一个「帮我安排一次出差」的 Agent，它需要处理机票预订（旅游域）、日历安排（个人生产力域）、报销预审（财务域）。你没办法把它拆成三个独立的 Agent 然后说每个只做一件事，因为它们需要在一个上下文中做联合决策。

「接口优先设计」——先定义 API，再实现服务。在 Agent 架构里，你定义的不是 API，而是「工具描述」和「目标约束」。工具描述告诉 Agent 能用什么能力，目标约束告诉 Agent 应该往哪个方向走。但 Agent 具体怎么组合这些工具来实现目标，是在运行时动态决定的。接口设计从「定义行为」变成了「定义能力边界」。

**在增强的原则：**

「关注点分离」——把复杂问题分解成独立关注的层面。这个原则在 Agent 架构里不但没失效，反而变得更加关键。因为 Agent 带来的不确定性要求你把系统分成「可验证的确定性层」和「不可验证的推理层」。底层：数据库、支付、认证——这些必须保持确定性，用传统的微服务方式管理。上层：Agent 推理、工具编排、决策链路——这些允许不确定性，通过约束边界来控制风险。

「可观测性」——你知道系统里正在发生什么。这个原则从「重要」变成了「生死攸关」。在微服务架构里，你可能靠日志和 metrics 就够了。在 Agent 架构里，你必须能追踪到 Agent 的每一步推理过程——它为什么选择了工具 A 而不是工具 B？它的推理链条中有没有什么危险的假设？这要求在架构层面设计新的可观测性机制。

## 2025 年值得注意的四个技术信号

这个话题容易写成科幻小说，所以我尽量讲已经发生的事和正在发生的事。

**信号一：Anthropic 的 MCP 协议在 2024 年 11 月推出，到 2025 年 Q1 已有超过 1000 个社区贡献的工具集成。** MCP（Model Context Protocol）试图标准化 LLM 与外部工具之间的通信方式——相当于 Agent 世界的「HTTP 协议」。如果这个协议或类似的协议成为行业标准，意味着 Agent 与工具之间的互操作性将不再是一个需要自己造轮子的问题，就像 HTTP 标准化之后 web 才真正爆发一样。

**信号二：Stripe 发布了面向 Agent 的支付 API。** 这个信号非常有意思。Stripe 作为一家支付基础设施公司，开始专门设计给 Agent 使用的 API——不是给人用的文档，是给 LLM 解析的机器可读接口描述。这意味着一线的基础设施公司已经在认真对待「Agent 是你的另一个用户群体」这件事了。

**信号三：Google 的 Agent2Agent 协议在 2025 年 4 月发布。** 这个协议试图解决的是 Agent 之间的通信标准化问题——不同厂商的 Agent 如何互相发现、协商任务、传递上下文。这是多 Agent 系统走向大规模的必经之路。

**信号四：OpenAI 的 Operator 和 Codex CLI。** 这两个产品代表的是「Agent 直接操作你的环境」的方向。Operator 可以操作浏览器完成复杂的多步骤任务，Codex CLI 可以在你的终端里直接执行编程任务。它们不是「帮你写代码」的工具，而是「帮你完成整个开发任务」的 Agent。

这些信号加起来告诉我一件事：Agent 架构不是在实验阶段，它正在进入基础设施建设阶段。而基础设施建设一旦完成，「为什么不用 Agent」就会从一个探索性的问题变成一个竞争性的问题。

## 对你来说这意味着什么

如果你是一个一线开发者：不要把 Agent 架构当作一个「以后可能会火」的概念。开始尝试用 Agent 框架做至少一个实际项目，哪怕只是内部工具。你不需要成为专家，但你需要理解 Agent 的行为模式和它的故障模式。这两样东西和传统软件的行为模式和故障模式完全不同。

如果你是一个架构师：重新审视你的当前系统中有哪些模块天然适合 Agent 化——需求多变、逻辑难以穷举、需要跨系统协同的场景。也重新审视哪些模块必须保持确定性——支付、认证、核心业务规则。把这两类模块的边界定义清楚，这个边界就是你未来三到五年的架构路线图。

如果你是一个技术管理者：关注你的团队里有谁在主动学习 Agent 相关技术。这些人是你的团队在未来架构转型时的关键资源。同时开始思考一个问题：如果你现有的系统要从「微服务为主」转变为「Agent 编排为主 + 微服务为底座」，你的团队需要什么样的新角色和新技能。

这次的范型转移不会在一两年内完成。微服务从概念到主流用了差不多八到十年。Agent 架构也差不多——但你不需要等到它成为主流才开始准备。十年前第一批深入理解微服务架构的工程师，在之后的招聘市场上获得了巨大的溢价。同样的窗口正在打开。

---

*下一篇：[08 ｜ IT 从业者的新高地——哪些垂直领域值得进入](./08-new-frontiers.md)*
