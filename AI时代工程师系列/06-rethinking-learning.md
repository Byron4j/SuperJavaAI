# 06 ｜ 学习路径的重构——当 AI 能教你一切

2024 年有个数据让我愣了好一会儿。Coursera 的年度报告说，2023 年到 2024 年间，AI 相关的课程注册量翻了四倍。同一时期，课程完成率从 2023 年的约 15% 掉到了不到 9%。

更多的人在学，更少的人学完了。

这个数字背后是一个正在系统性失效的学习模式。当知识获取的成本趋近于零的时候，「学什么」和「学到什么程度」变成了比「去哪学」更难的问题。而这恰恰是大多数人的学习策略没有跟上变化的地方。

## 知道型知识 vs 判断型知识

先做一个区分。我把它叫做：「知道型知识」和「判断型知识」。

**知道型知识**：你可以通过查询获取的东西。Redis 的淘汰策略有哪几种？Go 的 goroutine 调度器是什么模型？React 18 的 Concurrent Mode 怎么开启？这些问题的答案是可以被搜索、被 AI 回答、被文档覆盖的。你不需要记住它们，你需要的是知道它们存在，并在需要的时候能快速检索到。

**判断型知识**：无法通过查询获取的东西。什么时候不该用缓存？为什么这个场景下 event-driven 比 request-response 更好？这个 PR 的改动是否引入了一个不可观测的耦合？这些问题没有标准答案，它们依赖的是你在特定上下文中对 tradeoff 的判断。

AI 时代的核心矛盾在于：AI 极大地降低了「知道型知识」的价值，但现代教育体系——包括大学课程、在线课程、培训——仍然大量围绕「知道型知识」在运转。你花了四十个小时学完了一门「系统设计」课程，但学完后你面对真实的系统设计问题，还是不知道从哪里开始。为什么？因为课程教给你的是「知道型知识」——知道有负载均衡、知道有缓存策略、知道有数据库分片。但它没教给你判断型知识——在你的具体场景下，判断该用哪种。

这不是课程的错。判断型知识很难通过「教」来传递，它更多的是通过「做」来习得。这不是一个观点，是一个认知科学的结论。「判断」本质上是一种模式识别能力——你在过去经历过足够多的类似场景，你的大脑在潜意识层面建立了一个「这种情况下、那类方案会出问题」的模式匹配机制。这个机制只能通过积累经验来训练，无法通过阅读来灌输。

## 三种学习策略的转变

基于这个区分，AI 时代的学习策略需要在三个层面上重构。

### 从广度优先到深度优先

过去二十年流行的学习策略是广度优先。学一个新技术栈，先看完官方文档的 Getting Started，然后跑通官方 Tutorial，然后尝试做一个 Todo App，然后觉得「我会了」。下一个。

这个策略在 AI 时代彻底失效了。因为「广度」这件事，AI 可以比你做得更快、更全。你花一个周末「学了」React 的基础，但任何会写 prompt 的人都可以在半小时内让 AI 帮他写一个简单的 React 组件。你在广度上的投入，产出比已经低到不值得了。

深度优先是什么？选一个方向，不是学到「能写」为止，而是学到「能判断」为止。不是「我能在 React 里写一个表单」，而是「我能在五种实现表单的方式里，根据这个项目的具体约束，判断哪一种是对的」。这个深度的知识才是 AI 难以替代的。

具体操作上：每一个学习周期只定一个主题，比如「数据库索引」。不要两天就觉得自己学完了。去找对应的源码实现读——MySQL 的 B+ 树怎么实现的，PostgreSQL 的 GIN 索引和 B-tree 在什么场景下差异显著。去写测试验证你的理解——造一个百万行的表，分别用不同索引跑查询，看执行计划，看 I/O 差异。去读几个大公司的生产事故报告，找出和索引相关的那几个，理解在生产压力下索引会怎么出问题。做完这些，你对「数据库索引」的认知会和「看过文档」的人完全不在一个量级。

最重要的是，这种深度学习建立的是判断力。下回有人跟你说「这个查询慢，我们加个索引吧」，你会本能地反问：「什么查询？WHERE 条件是什么？数据分布怎样？这个表是读多写少还是读写均衡？」因为你知道索引不是免费的——这个判断，不是从文档里看来的，是从你造数据跑测试时亲眼看到写入性能下降中得来的。

### 从课程驱动到项目驱动

在线教育的商业模式决定了它必须把知识包装成「可消费」的形式——视频、测验、证书。这套形式适合传递知道型知识，但获取判断型知识的最佳方式是项目驱动。

你不需要再报一门「Go 语言高级编程」的课。你真正需要的是找一个你有点怕做不出来的项目，然后用 Go 把它做出来。过程中你会被迫面对真实的问题——并发竞争、内存泄漏、性能 profiling、错误处理策略——这些都不是任何课程能完整覆盖的，因为它们取决于你项目里的具体上下文。

项目驱动的另一个隐性好处是：它给你提供了失败的空间。课程设计的目标是让你「成功完成」，所以它帮你避开了所有坑。但判断力恰好是在掉进坑里、再爬出来的过程中形成的。一个好的学习项目，失败率应该在 30% 到 50% 之间——如果你每次都能顺利做完，说明项目对你来说太简单了。

### 从输入驱动到输出驱动

多数人的学习模式是输入驱动：买书、收藏文章、订阅 newsletter、看视频。输入是舒服的——它给你一种「我在进步」的错觉，而实际上你可能只是在消费内容。但知识留存率的惨淡数字不会骗人：听完一堂课后 48 小时内，大多数人会忘记 70% 以上的内容。这不是记忆力的问题，是学习方式的问题。

输出驱动是把顺序反过来：你先决定你要输出什么——一篇技术文章、一次内部分享、一个开源项目、一个技术方案文档——然后为了高质量地完成这个输出，你被倒逼着去深入学习相关的内容。输出驱动之所以有效，是因为它制造了「必要性」——你不需要「自律」去学习，你需要学习来完成你已经承诺了的输出。

具体的操作建议：每个月至少有一次公开的技术输出。不用写得多长，不用覆盖多广——就写一个你最近在工作中解决的真实技术问题。三五百字就行。但必须写清楚三个东西：遇到了什么问题、为什么它是问题、解决方案背后的判断逻辑是什么。坚持半年，你会发现你对技术的理解从「表面知道」进入了「深层理解」——因为写作这个过程本身就在迫使你组织思维、发现逻辑漏洞、查证你模糊不清的假设。

这条建议不是鸡汤。我见过的最好的工程师，几乎都有一个共同的习惯：持续性、有规律的输出。不是因为他们本来就很强所以才输出，而是因为输出的过程让他们变得很强。

## 学习不该是你一个人的事

最后说一个很多人不重视但极其有效的策略：把你的学习过程公开化。

公开不是说你要变成一个知识网红。而是说，当你学一个新东西的时候，不要只在自己的笔记本上写下来。把学习笔记发到公司内部的 Wiki 上，发到技术社区里，哪怕是发到你的个人博客上只有十个人看。公开学习有三个好处：

第一，它会倒逼你写清楚。你在笔记本上可以写半句话，因为你知道未来的自己能看懂。但发布到公共空间，你必须把逻辑链条完整地写出来。这个「完整性倒逼」本身就是一种深度学习。

第二，它会引来纠正。你写的东西可能不对，可能不全面，可能过时了。如果只有你自己看，你永远不会发现。但如果发到公开空间，总会有人指出来。被纠正的感觉不好受，但被纠正一次学到的东西，是「独自正确」永远给不了你的。

第三，它会变成你的资产。一年后你回头看，那些公开的学习笔记和研究记录，比你的简历更有说服力。一个面试官可以怀疑你简历上写的「熟悉分布式系统」，但没法怀疑你博客上那些写了两年、包含具体事故分析和架构决策推演的技术文章。

AI 能降低知识获取的成本，但你的学习深度、广度选择、输出习惯——这些是你和 AI 之间真正的差别。在一个知识唾手可得的时代，「你怎么学」比「你学了什么」重要得多。

---

*下一篇：[07 ｜ 从微服务到智能体——AI 如何改变技术架构](./07-microservices-to-agents.md)*
